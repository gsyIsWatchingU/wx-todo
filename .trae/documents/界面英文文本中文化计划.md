# 界面英文文本中文化计划

## 1. 需求分析

用户希望项目界面中的英文文本全部改为中文，提升用户体验。

## 2. 待修改文件清单

### 2.1 src/pages/index/index.tsx
- "Todo List" → "待办事项"

### 2.2 src/pages/today/index.tsx
- "Day", "Week", "Month", "List" → "日", "周", "月", "列表"
- "Search title or notes" → "搜索标题或备注"
- "Filter", "Hide" → "筛选", "隐藏"
- "Status", "Priority", "List", "Date range" → "状态", "优先级", "列表", "日期范围"
- "All", "Active", "Completed" → "全部", "进行中", "已完成"
- "All priorities", "P1", "P2", "P3" → "所有优先级", "P1", "P2", "P3"
- "All lists" → "所有列表"
- "Current view", "Custom range", "No date limit" → "当前视图", "自定义范围", "无日期限制"
- "Start date", "End date" → "开始日期", "结束日期"
- "Reset", "Done" → "重置", "完成"
- "Today" → "今天"
- "Loading..." → "加载中..."
- "{tasks.length} tasks" → "{tasks.length} 个任务"
- "Search: {keyword}" → "搜索: {keyword}"
- "Status: {status}" → "状态: {status}"
- "Priority: P{priority}" → "优先级: P{priority}"
- "List: {list.name}" → "列表: {list.name}"
- "Date: {customDateStart} to {customDateEnd}" → "日期: {customDateStart} 至 {customDateEnd}"
- "Date: none" → "日期: 无"
- "Quick add a task..." → "快速添加任务..."
- "Add" → "添加"
- Toast消息："Failed to load tasks" → "加载任务失败"
- Toast消息："Task created" → "任务创建成功"
- Toast消息："Failed to create task" → "创建任务失败"
- Toast消息："Failed to update task" → "更新任务失败"

### 2.3 src/pages/today/components/DayView.tsx
- "Today" → "今天"
- "No due date" → "无截止日期"
- "Scheduled" → "已安排"
- "No tasks match these filters." → "没有匹配的任务。"

### 2.4 src/pages/today/components/ListView.tsx
- "No tasks match these filters." → "没有匹配的任务。"

### 2.5 src/utils/dateTools.ts
- "Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun" → "一", "二", "三", "四", "五", "六", "日"

### 2.6 src/pages/task-edit/index.tsx
- "添加一些描述..." → "添加描述..."

## 3. 实施步骤

1. 修改 `src/pages/index/index.tsx`
2. 修改 `src/pages/today/index.tsx`
3. 修改 `src/pages/today/components/DayView.tsx`
4. 修改 `src/pages/today/components/ListView.tsx`
5. 修改 `src/utils/dateTools.ts`
6. 修改 `src/pages/task-edit/index.tsx`

## 4. 风险与注意事项

- 确保修改后应用能正常运行
- 注意保留变量和逻辑不变，只修改显示文本